# WKS-1535 native-client certification receipt

Date: 2026-09-02
Branch: `dallascrilley/wks-1535-certify-bb-native-clients-pairing-reconnect-pending`

## Scope

This receipt records a physical Samsung candidate install, Direct-mode behavior, bb connect protocol behavior, the physical-iPhone build gate, and exact local cleanup. No pairing credentials, cookies, tokens, machine credentials, or server secrets are recorded.

This is an evidence-backed partial receipt, not a cross-platform certification pass.

## Android candidate and recovery

The repository-owned dependency repair is `patches/@react-native-cookies__cookies@6.2.1.patch`; it replaces both obsolete `jcenter()` repository entries with `mavenCentral()`. `pnpm install --frozen-lockfile` applied the patch successfully, with the repository's pre-existing missing generated desktop/daemon bin warnings.

The arm64 release command completed successfully:

```text
cd apps/mobile/android && ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools ./gradlew app:assembleRelease --no-daemon -PreactNativeArchitectures=arm64-v8a
```

Candidate artifact:

- Path: `apps/mobile/android/app/build/outputs/apk/release/app-release.apk`
- Size: `65426699` bytes
- SHA-256: `d4055cc0480715e09765a0ee8b68cdf52045a8f41e673dbf1662e65d1e3341b2`
- APK package: `app.getbb.mobile`
- Version: `0.39.0` (`versionCode=1`)
- Native ABI: `arm64-v8a`
- Signature: APK v2, Android debug certificate SHA-256 `fac61745dc0903786fb9ede62a962b399f7348f0bb6f899b8332667591033b9c`

Before mutation, the installed APK was pulled to the session's external evidence directory. Its SHA-256 was `b71fae21612b3a9c66197f13c5afc3f7feb618c3d407271742edebc8f88d27c0`; its Android signing marker matched the candidate's `[51ed3f60]` marker. The candidate was installed with data-preserving `adb install -r`, the saved original APK was reinstalled with `adb install -r` to prove rollback, and the candidate was installed again with `adb install -r`. Each install returned `Success`; no data-clear or uninstall command ran.

After the final install, a fresh pull of the tablet's active `base.apk` was `65426699` bytes and had SHA-256 `d4055cc0480715e09765a0ee8b68cdf52045a8f41e673dbf1662e65d1e3341b2`, exactly matching the candidate. The package still reported first install time `2026-08-29 00:27:41`, version `0.39.0`, and final update time `2026-09-02 20:40:03`, so the update did not replace the app identity or clear package data.

## Samsung physical-device behavior

Device: physical Samsung SM-X800, hardware serial `R52T50B4F1D`, reached through the paired ADB transport.

### Direct mode

- The release candidate cold-launched `app.getbb.mobile/.MainActivity`, not the Expo development launcher.
- The native Servers screen retained both saved Direct endpoints across candidate install, proven rollback, and final candidate install.
- A Direct endpoint rendered the existing Rich thread. During an exact owned backend-process outage, the same rendered thread remained on screen. After the backend restarted at the same endpoint, the same Rich thread recovered without adding or changing the server profile.
- The recovered Rich thread displayed `Thread needs user input`. After answering the pending interaction, that marker was absent from the next UI hierarchy capture while the same thread content remained rendered.
- After the final candidate install, the Servers screen still listed the saved Direct endpoint and marked it selected. This proves Direct profile/update survival on the Samsung.

### bb connect

- The repository's mobile connect stub and a temporary local protocol adapter exercised the documented machine-code flow without bypassing the native client.
- Initial pairing persisted a native `bb connect` server profile. Revoking the machine through the stub invalidated the prior machine/session and the client changed to a sign-in failure state.
- The native `Paired again` flow completed and listed account servers, proving machine-code re-enrollment and account-server discovery at the protocol boundary.
- The resulting WebView did not render a connected session on the physical `.localhost`/ADB-reverse setup. It instead showed `Cannot reach this server` and `The session was minted but the server keeps rejecting it`. No server-side cookie injection or authentication bypass was used.

The initial HTTPS run established the native pairing/profile behavior but exposed a physical Android trust/cookie boundary before the rendered session could be exercised. The fresh Samsung continuation below supersedes that interim limitation for the Android client; iPhone remains separately blocked by signing.

## Fresh Samsung bb connect continuation

Date: 2026-09-03

The connect stub now has an opt-in `BB_MOBILE_E2E_STUB_PROTOCOL=http` mode. HTTPS remains the default. For this physical Android run, the stub used that mode with the temporary host-only `BB_MOBILE_E2E_COOKIE_DOMAIN=stub.localhost` setting so the native WebView could carry its local session cookie. The test backend served the rendered app at the same upstream endpoint. No product authentication or cookie injection bypass was used.

On the physical Samsung SM-X800, the native flow and rendered bb connect path then passed end to end:

- Initial pairing rendered `Paired with bb connect`, the stub server URL, and `Signed in. Session renews automatically.`
- The seeded `Rich thread` rendered through the connected session, including the long Markdown content and a pending question. Selecting `Staging` and submitting it rendered `Question answered: staging`.
- The control endpoint expired the active desktop sessions (`expired=2`, `closed=4`). The client renewed its desktop session and restored the same thread; the stub observed new authenticated proxy traffic.
- Removing only the exact ADB reverse caused the physical app to render `Cannot reach this server` with the direct `java.net.ConnectException` failure. Restoring that exact reverse reconnected the app and rendered the same Rich thread and answered question.
- Revoking the machine produced the physical `This server needs pairing again` / `Desktop session not authorized` state. The native `Pair again` flow completed, returned to an authenticated account-server screen, and `Done` returned to the same rendered Rich thread.
- Reinstalling the exact candidate APK with data-preserving `adb install -r` returned `Success`. After a cold relaunch, the app rendered the same Rich thread and `Question answered: staging`; the stub recorded another authenticated session. The candidate remained the exact `d4055cc0480715e09765a0ee8b68cdf52045a8f41e673dbf1662e65d1e3341b2` hash, package data was retained, and first-install time remained unchanged.

The final stub readback after re-pair and update showed two machines (one revoked, one active), two active sessions for the active machine, `redeems=2`, `desktopSessions=8`, `listServers=2`, `proxiedRequests=354`, `proxiedUpgrades=27`, `unauthenticatedRequests=8`, and `unauthenticatedUpgrades=20`, with two live sockets. These counters are supporting protocol evidence; the physical UI observations above are the behavior proof.

## Physical iPhone gate

The physical iPhone 14 Plus was visible and paired to Xcode-beta as UDID `00008110-000C3D263C83401E`. The release bundle completed, but device build/install stopped with Xcode exit 65 because the personal development team cannot create an iOS development provisioning profile for `app.getbb.mobile` with the Associated Domains and Push Notifications capabilities. Xcode also reported no matching provisioning profile.

No iPhone build was installed, and no physical-iPhone pairing, reconnect, pending interaction, revocation, or update-survival behavior is claimed. Resolving Apple signing/provisioning is an explicit human account/device gate.

## Repository checks

- `pnpm exec turbo run test:smoke --filter=@bb/integration-tests`: passed, 10 files / 28 tests.
- `pnpm exec turbo run test --filter=@bb/integration-tests`: not clean, 20 files / 71 tests passed and 6 files / 6 tests failed by the existing 60-second test timeout in fake environment/multi-thread coverage. No device claim is based on this suite.
- `pnpm exec turbo run typecheck lint test --filter=@bb/mobile --force`: passed, with typecheck and tests clean; 43 files / 326 tests passed. Lint completed with four pre-existing warnings in `WebViewSpikeScreen.tsx` and `useShellBridge.ts`.

## Cleanup and recovery

- Removed only the temporary Tailscale Serve handler on HTTPS port 8450. The pre-existing default, 8443, 8444, 8445, and Funnel 10000 handlers remained present.
- Removed only the temporary Samsung ADB reverse for device TCP port 8451; `adb reverse --list` was empty afterward.
- Stopped the exact captured mobile E2E backend, connect stub, and temporary protocol-adapter process trees. Ports 41999, 42997, 42999, 8450, and 8451 had no remaining listeners or connections afterward.
- The saved original APK remains in the external session evidence directory for rollback. The tablet currently runs the exact candidate hash above, and app data was preserved.

## Acceptance status

| Behavior | Samsung | iPhone |
| --- | --- | --- |
| Direct pairing/profile | Passed: saved Direct endpoint rendered the candidate | Unverified: no signed install |
| Direct same-thread reconnect | Passed: exact backend outage and recovery, same Rich thread | Unverified: no signed install |
| Direct pending interaction | Passed: pending marker appeared and cleared after answer | Unverified: no signed install |
| bb connect pairing | Passed: native pairing and rendered connected session | Unverified: no signed install |
| bb connect reconnect/pending | Passed: expiry renewal, reverse outage/recovery, and pending answer through rendered bb connect | Unverified: no signed install |
| Revocation | Passed: machine/session invalidation and native re-pair returned to the rendered session | Unverified: no signed install |
| Update and pairing-data survival | Passed for Samsung Direct and bb connect on the exact candidate APK | Unverified: no signed install |

## Physical iPhone bb connect continuation

Date: 2026-09-03

This continuation supersedes the earlier signing-only gate for the installed physical iPhone client. No pairing credential, cookie, token, machine identifier, or user-thread content is recorded here.

- The iPhone Mirroring session reached the physical iPhone 14 Plus and opened the installed bb client.
- The newly paired bb connect machine was revoked from the authenticated getbb.app dashboard. The dashboard then showed no registered machine.
- The iPhone client visibly changed to `This server needs pairing again` and `Desktop session not authorized`, proving that the exact revoked credential lost access.
- A fresh, one-time code generated from the desktop Remote access settings completed native `Sign in again`. The iPhone rendered `Paired … again`, `Signed in. Session renews automatically.`, and its account default server.
- Selecting `Done` returned to the authenticated rendered bb workspace, including the recent-thread list and composer. No thread was opened, changed, or submitted.
- Force-closing the iPhone bb client and launching it again returned to that authenticated rendered workspace without another sign-in.

The original saved Direct profile was not certified in this continuation. Its configured endpoint returned HTTP 502 because its local Tailscale Serve upstream was absent; that separate route was preserved rather than changed.

For a bounded Direct-route probe, an owned temporary tailnet-only Tailscale Serve handler on port 8452 proxied the running local bb server and returned HTTP 200. The iPhone UI did not reach Direct-profile setup before a view-size change made its navigation unreliable, so no Direct behavior is claimed. The exact port 8452 handler was removed immediately afterward; the final Serve status contains no handler at that port.

## Updated acceptance status

| Behavior | Samsung | iPhone |
| --- | --- | --- |
| Direct pairing/profile | Passed: saved Direct endpoint rendered the candidate | Unverified: saved Direct endpoint returned HTTP 502 from an absent local Serve upstream |
| Direct same-thread reconnect | Passed: exact backend outage and recovery, same Rich thread | Unverified |
| Direct pending interaction | Passed: pending marker appeared and cleared after answer | Unverified |
| bb connect pairing | Passed: native pairing and rendered connected session | Passed: native pairing and rendered authenticated workspace |
| bb connect reconnect/pending | Passed: expiry renewal, reverse outage/recovery, and pending answer through rendered bb connect | Unverified |
| Revocation | Passed: machine/session invalidation and native re-pair returned to the rendered session | Passed: dashboard revocation, client authorization failure, native re-pair, and rendered recovery |
| Update and pairing-data survival | Passed for Samsung Direct and bb connect on the exact candidate APK | Unverified |

WKS-1535 remains incomplete only for the iPhone Direct route, same-thread reconnect, pending interaction, and pairing-data survival across an app update.
